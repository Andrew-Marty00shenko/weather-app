import { shallowEqual, useSelector } from "react-redux";

const SelectedCities = () =>{
    // const currentCity = useSelector(state=> state.weatherReducer.currentWeather, shallowEqual);

    return <div>
        <p>favorites</p>
    </div>
}

export default SelectedCities;